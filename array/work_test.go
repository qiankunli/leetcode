package array

import (
	"fmt"
	"testing"
)

func TestMax2Profit(t *testing.T) {
	//cost := []int{40, 20, 30}
	//profit := []int{30, 20, 20}
	//fmt.Println(Max2Profit(cost, profit, 50))

	s := fmt.Sprintf("%d", 789)
	for _, c := range s {
		fmt.Println(c - '1' + 1)
	}
}
