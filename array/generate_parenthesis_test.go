package array

import (
	"fmt"
	"testing"
)

func TestGenerateParenthesis(t *testing.T) {
	result := GenerateParenthesis(3)
	fmt.Println(result)

	//result := GenerateParenthesis(1)
	//fmt.Println(result)
}
