package array

import (
	"fmt"
	"testing"
)

func TestCanPartition(t *testing.T) {
	//nums := []int{1, 2, 3, 4}
	//fmt.Println(CanPartition(nums))
	s := "abc"
	for i, v := range s {
		fmt.Println(i, v)
	}
}
